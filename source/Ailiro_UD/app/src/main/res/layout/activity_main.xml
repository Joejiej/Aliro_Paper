<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mainLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="32dp"
    android:background="?attr/colorSurface"
    tools:context=".MainActivity">

    <!-- Logo 图片 -->
    <ImageView
        android:id="@+id/logoView"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:src="@drawable/ic_aliro_logo"
        android:contentDescription="Aliro Logo"
        android:layout_marginBottom="24dp"
        android:scaleType="centerInside" />

    <!-- 标题 -->
    <TextView
        android:id="@+id/titleView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Aliro"
        android:textSize="26sp"
        android:textStyle="bold"
        android:textColor="@color/titleTextColor"
        android:fontFamily="sans-serif-medium"
        android:layout_marginBottom="40dp"
        android:textAlignment="center" />

    <!-- 开始认证按钮 -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/startAuthButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="开始认证"
        android:textColor="@color/buttonTextColor"
        android:layout_marginBottom="16dp"
        app:cornerRadius="16dp"
        app:backgroundTint="@color/buttonStartAuthBackground"
        app:iconPadding="0dp" />

    <!-- 生成公钥按钮 -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/genKeyButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="生成公钥"
        android:textSize="18sp"
        android:icon="@drawable/ic_key"
        app:iconPadding="8dp"
        app:iconGravity="textStart"
        android:textColor="@color/buttonTextColor"
        app:cornerRadius="16dp"
        app:backgroundTint="@color/buttonGenKeyBackground" />

</LinearLayout>
